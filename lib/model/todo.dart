class Todo{
  late String id;
  late String title;
  late String description;
  late bool complete;

  Todo(
      {required this.id,
        required this.title,
        required this.description,
        required this.complete});
}